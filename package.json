{
  "name": "lumi-books-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "npm --prefix apps/api run start:dev",
    "dev:worker": "npm --prefix apps/worker run start:dev",
    "test:web": "npm --prefix apps/web run lint && npm --prefix apps/web test",
    "test:api": "npm --prefix apps/api run typecheck && npm --prefix apps/api test",
    "test:worker": "npm --prefix apps/worker run typecheck",
    "test": "npm run test:web && npm run test:api && npm run test:worker",
    "build:web": "npm --prefix apps/web run build",
    "build:api": "npm --prefix apps/api run build",
    "build:worker": "npm --prefix apps/worker run build",
    "build": "npm run build:web && npm run build:api && npm run build:worker",
    "verify": "npm test && npm run build",
    "smoke:api": "npm --prefix apps/api run smoke"
  }
}
